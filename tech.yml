# MongoDB
spring:
  data:
    mongodb:
      password: tech_user
      uri:

# Logging
logging:
  level:
    com.example.tech: INFO
    org.springframework: INFO

# Debug mode
  debug:
    enable: false